# **Appgate - Geolocation IP**

### **Run Local**
Execute the next command on command line console
```
    ./run-local
```

### **Run Docker**
Execute the next command on command line console
```
    ./run-docker
```
